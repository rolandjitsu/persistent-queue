# Project-specific spellings that the typos dictionary would otherwise flag.
# Add words here when `typos` reports a false positive (e.g. an identifier it
# reads as a misspelling). None needed yet.
[default.extend-words]
